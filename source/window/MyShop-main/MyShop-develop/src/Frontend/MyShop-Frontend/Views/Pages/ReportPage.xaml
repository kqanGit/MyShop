<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop_Frontend.Views.Pages.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop_Frontend.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#5D5FEF"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#22C55E"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="24" RowSpacing="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ROW 0: HEADER AND FILTERS -->
            <Border Grid.Row="0" Background="White" CornerRadius="16" Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE9F9;" FontSize="24" Foreground="{StaticResource PrimaryColor}"/>
                        <StackPanel>
                            <TextBlock Text="Business Reports" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Text="Analyze your store performance" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="12">
                        <StackPanel>
                            <TextBlock Text="From Date" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                            <CalendarDatePicker x:Name="FromDatePicker"
                                                Date="{x:Bind ViewModel.FromDate, Mode=TwoWay}"
                                                DateFormat="{}{day.integer}/{month.integer}/{year.full}"
                                                MinWidth="150"/>
                        </StackPanel>

                        <TextBlock Text="to" VerticalAlignment="Bottom" Margin="0,0,0,10" Foreground="{StaticResource TextSecondary}"/>

                        <StackPanel>
                            <TextBlock Text="To Date" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                            <CalendarDatePicker x:Name="ToDatePicker"
                                                Date="{x:Bind ViewModel.ToDate, Mode=TwoWay}"
                                                DateFormat="{}{day.integer}/{month.integer}/{year.full}"
                                                MinWidth="150"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Group By" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4"/>
                            <ComboBox x:Name="GroupByCombo" SelectedIndex="{x:Bind ViewModel.SelectedGroupBy, Mode=TwoWay}" MinWidth="120">
                                <ComboBoxItem Content="By Day"/>
                                <ComboBoxItem Content="By Week"/>
                                <ComboBoxItem Content="By Month"/>
                                <ComboBoxItem Content="By Year"/>
                            </ComboBox>
                        </StackPanel>

                        <Button Background="{StaticResource PrimaryColor}" Foreground="White" CornerRadius="8" Padding="16,8" VerticalAlignment="Bottom" Click="ApplyFilter_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                <TextBlock Text="Generate Report"/>
                            </StackPanel>
                        </Button>

                        <Button Background="#F1F5F9" CornerRadius="8" Padding="16,8" VerticalAlignment="Bottom" Click="ExportReport_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE78C;" FontSize="14" Foreground="#475569"/>
                                <TextBlock Text="Export" Foreground="#475569"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ROW 1: SUMMARY CARDS -->
            <Grid Grid.Row="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="16" Padding="20">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Background="#EEF2FF" CornerRadius="10" Padding="10">
                                <FontIcon Glyph="&#xE8C8;" FontSize="18" Foreground="{StaticResource PrimaryColor}"/>
                            </Border>
                            <TextBlock Text="Total Revenue" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.TotalRevenue, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.RevenueChangePercent, Mode=OneWay}" Foreground="{x:Bind ViewModel.RevenueChangeColor, Mode=OneWay}" FontSize="13"/>
                            <TextBlock Text="vs last period" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="16" Padding="20">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Background="#ECFDF5" CornerRadius="10" Padding="10">
                                <FontIcon Glyph="&#xE9D9;" FontSize="18" Foreground="{StaticResource SuccessColor}"/>
                            </Border>
                            <TextBlock Text="Total Profit" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.TotalProfit, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.ProfitChangePercent, Mode=OneWay}" Foreground="{x:Bind ViewModel.ProfitChangeColor, Mode=OneWay}" FontSize="13"/>
                            <TextBlock Text="vs last period" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="16" Padding="20">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Background="#FEF3C7" CornerRadius="10" Padding="10">
                                <FontIcon Glyph="&#xE7BF;" FontSize="18" Foreground="{StaticResource WarningColor}"/>
                            </Border>
                            <TextBlock Text="Total Orders" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.TotalOrders, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.AverageOrderValue, Mode=OneWay}" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                            <TextBlock Text="avg. order value" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Background="White" CornerRadius="16" Padding="20">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Border Background="#FCE7F3" CornerRadius="10" Padding="10">
                                <FontIcon Glyph="&#xE821;" FontSize="18" Foreground="#EC4899"/>
                            </Border>
                            <TextBlock Text="Products Sold" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.TotalProductsSold, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.UniqueProductsCount, Mode=OneWay}" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                            <TextBlock Text="unique products" Foreground="{StaticResource TextSecondary}" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ROW 2: REVENUE CHART -->
            <Border Grid.Row="2" Background="White" CornerRadius="16" Padding="24">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="350"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="24">
                        <TextBlock Text="Revenue and Profit Trend" FontSize="18" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Spacing="24">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="12" Height="12" Fill="{StaticResource PrimaryColor}"/>
                                <TextBlock Text="Revenue" Foreground="{StaticResource TextSecondary}" FontSize="14"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="12" Height="12" Fill="{StaticResource SuccessColor}"/>
                                <TextBlock Text="Profit" Foreground="{StaticResource TextSecondary}" FontSize="14"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{x:Bind ViewModel.RevenueProfitSeries, Mode=OneWay}"
                                        XAxes="{x:Bind ViewModel.XAxes, Mode=OneWay}"
                                        YAxes="{x:Bind ViewModel.YAxes, Mode=OneWay}"
                                        TooltipPosition="Top"
                                        ZoomMode="X"/>
                </Grid>
            </Border>

            <!-- ROW 3: TOP PRODUCTS AND CATEGORY -->
            <Grid Grid.Row="3" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="16" Padding="24">
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE734;" FontSize="18" Foreground="{StaticResource WarningColor}"/>
                            <TextBlock Text="Top Selling Products" FontSize="16" FontWeight="SemiBold"/>
                        </StackPanel>

                        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.TopProducts, Mode=OneWay}" SelectionMode="None" MaxHeight="350">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="0,12" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Background="#EEF2FF" CornerRadius="8" Width="32" Height="32">
                                            <TextBlock Text="{Binding Rank}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#5D5FEF"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ProductName}" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding QuantitySoldText}" Foreground="#64748B" FontSize="12"/>
                                        </StackPanel>

                                        <TextBlock Grid.Column="2" Text="{Binding RevenueText}" FontWeight="SemiBold" Foreground="#22C55E" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="16" Padding="24">
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE9F9;" FontSize="18" Foreground="{StaticResource PrimaryColor}"/>
                            <TextBlock Text="Sales by Product" FontSize="16" FontWeight="SemiBold"/>
                        </StackPanel>

                        <lvc:PieChart Grid.Row="1" Series="{x:Bind ViewModel.CategorySeries, Mode=OneWay}" LegendPosition="Right" MaxHeight="350"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- ROW 4: PERIOD COMPARISON TABLE -->
            <Border Grid.Row="4" Background="White" CornerRadius="16" Padding="24">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE787;" FontSize="18" Foreground="{StaticResource PrimaryColor}"/>
                        <TextBlock Text="Period Comparison" FontSize="16" FontWeight="SemiBold"/>
                    </StackPanel>

                    <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.PeriodComparisons, Mode=OneWay}" SelectionMode="None">
                        <ListView.Header>
                            <Grid Padding="16,12" Background="#F8FAFC" CornerRadius="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Period" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Revenue" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                <TextBlock Grid.Column="2" Text="Profit" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                <TextBlock Grid.Column="3" Text="Orders" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                <TextBlock Grid.Column="4" Text="Growth" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="16,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding PeriodLabel}" FontWeight="Medium"/>
                                    <TextBlock Grid.Column="1" Text="{Binding RevenueText}" HorizontalAlignment="Right"/>
                                    <TextBlock Grid.Column="2" Text="{Binding ProfitText}" HorizontalAlignment="Right"/>
                                    <TextBlock Grid.Column="3" Text="{Binding OrderCount}" HorizontalAlignment="Right"/>
                                    <TextBlock Grid.Column="4" Text="{Binding GrowthText}" Foreground="{Binding GrowthColor}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
