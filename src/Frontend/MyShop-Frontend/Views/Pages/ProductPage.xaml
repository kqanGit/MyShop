<Page
    x:Name="PageRoot"
    x:Class="MyShop_Frontend.Views.Pages.ProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop_Frontend.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyShop_Frontend.ViewModels.Products"
    xmlns:converters="using:MyShop_Frontend.Helpers.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ProductViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <converters:PriceConverter x:Key="PriceConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Product" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Browse products" FontSize="14" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                <Button Command="{Binding ImportCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE898;"/>
                        <TextBlock Text="Import"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding AddCategoryCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8D2;"/>
                        <TextBlock Text="New Category"/>
                    </StackPanel>
                </Button>
                <Button Background="#111827" Foreground="White" CornerRadius="10" Padding="14,10" Command="{Binding AddProductCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;"/>
                        <TextBlock Text="New Product" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filters -->
        <Grid Grid.Row="1" ColumnSpacing="12" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" PlaceholderText="Search by product name..." Text="{Binding Keyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" DisplayMemberPath="CategoryName"/>
            <ComboBox Grid.Column="2" SelectedValuePath="Tag" SelectedValue="{Binding Sort, Mode=TwoWay}">
                <ComboBoxItem Content="Name (A-Z)" Tag="name_asc"/>
                <ComboBoxItem Content="Price (Low->High)" Tag="price_asc"/>
                <ComboBoxItem Content="Price (High->Low)" Tag="price_desc"/>
                <ComboBoxItem Content="Stock (Low->High)" Tag="stock_asc"/>
            </ComboBox>
        </Grid>

        <!-- Price Range + Paging -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16" Spacing="8">
            <Grid ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,0,0,4">
                        <TextBlock Text="Min:" Foreground="Gray"/>
                        <TextBlock Text="{Binding MinPriceFilter, Converter={StaticResource PriceConverter}}" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Slider Minimum="0" Maximum="1000000" Value="{Binding MinPriceFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,0,0,4">
                        <TextBlock Text="Max:" Foreground="Gray"/>
                        <TextBlock Text="{Binding MaxPriceFilter, Converter={StaticResource PriceConverter}}" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Slider Minimum="0" Maximum="1000000" Value="{Binding MaxPriceFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <TextBlock Text="{Binding ShowingStatus}" VerticalAlignment="Center" Foreground="Gray"/>
                <AppBarSeparator/>
                <Button Content="&lt;" Command="{Binding PreviousPageCommand}"/>
                <Button Content="&gt;" Command="{Binding NextPageCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- Product Cards -->
        <ScrollViewer Grid.Row="3">
            <ItemsRepeater ItemsSource="{Binding Products}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="200" MinItemHeight="280" ItemsJustification="Start"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="8" BorderThickness="1" BorderBrush="#E5E7EB" Background="White" Margin="8" Padding="0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Image Area -->
                                <Grid Height="180">
                                    <Image Source="{Binding Image}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    
                                    <!-- Badges Overlay -->
                                    <Grid VerticalAlignment="Top" Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Category Badge -->
                                        <Border Grid.Column="0" Background="#F3F4F6" CornerRadius="4" Padding="6,2" Opacity="0.9">
                                            <TextBlock Text="{Binding CategoryName}" FontSize="10" Foreground="#374151" TextTrimming="CharacterEllipsis" MaxWidth="100"/>
                                        </Border>

                                        <!-- Stock Badge -->
                                        <Border Grid.Column="2" Background="#D1FAE5" CornerRadius="4" Padding="6,2" Opacity="0.9">
                                            <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="#065F46">
                                                <Run Text="Stock:"/>
                                                <Run Text="{Binding Stock}"/>
                                            </TextBlock>
                                        </Border>
                                    </Grid>
                                </Grid>

                                <!-- Content Area -->
                                <StackPanel Grid.Row="1" Padding="12" Spacing="8">
                                    <!-- Name -->
                                    <TextBlock Text="{Binding ProductName}" FontSize="14" TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis" Height="40" VerticalAlignment="Top"/>
                                    
                                    <!-- Price -->
                                    <TextBlock Text="{Binding Price, Converter={StaticResource PriceConverter}}" FontSize="16" FontWeight="Bold" Foreground="#EE4D2D" />

                                    <!-- Actions -->
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="4">
                                            <Button Background="Transparent" Padding="4" Command="{Binding ElementName=PageRoot, Path=DataContext.ViewProductCommand}" CommandParameter="{Binding}" ToolTipService.ToolTip="View Images">
                                                <FontIcon Glyph="&#xE7B3;" FontSize="14"/>
                                            </Button>
                                            <Button Background="Transparent" Padding="4" Command="{Binding ElementName=PageRoot, Path=DataContext.UpdateProductCommand}" CommandParameter="{Binding}">
                                                <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                                            </Button>
                                            <Button Background="Transparent" Padding="4" Command="{Binding ElementName=PageRoot, Path=DataContext.DeleteProductCommand}" CommandParameter="{Binding}">
                                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#EF4444"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
