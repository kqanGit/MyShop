<Page
    x:Name="PageRoot"
    x:Class="MyShop_Frontend.Views.Pages.ProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop_Frontend.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyShop_Frontend.ViewModels.Products"
    xmlns:converters="using:MyShop_Frontend.Helpers.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ProductViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <converters:PriceConverter x:Key="PriceConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Product Management" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Browse and manage supermarket products" FontSize="14" Foreground="Gray"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                
                <Button>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE898;"/>
                        <TextBlock Text="Import"/>
                    </StackPanel>
                </Button>
                <Button Background="Black" Foreground="White" CornerRadius="4" Command="{Binding AddProductCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;"/>
                        <TextBlock Text="New Product"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filters Row Removed -->

        <!-- Price Range (Simplified as slider) -->
        <StackPanel Grid.Row="2" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,0,0,4">
                <TextBlock Text="Max Price:" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding MaxPriceFilter, Converter={StaticResource PriceConverter}}" FontWeight="SemiBold"/>
            </StackPanel>
            <Slider Minimum="0" Maximum="1000000" Value="{Binding MaxPriceFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,8,0,0">
                <TextBlock Text="Page size:" VerticalAlignment="Center" Foreground="Gray"/>
                <ComboBox Width="90" SelectedValuePath="Content" SelectedValue="{Binding PageSize, Mode=TwoWay}">
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="10"/>
                    <ComboBoxItem Content="15"/>
                    <ComboBoxItem Content="20"/>
                    <ComboBoxItem Content="25"/>
                    <ComboBoxItem Content="30"/>
                </ComboBox>
                <TextBlock Text="{Binding ShowingStatus}" VerticalAlignment="Center" Foreground="Gray"/>
                <AppBarSeparator/>
                <Button Content="&lt;" Command="{Binding PreviousPageCommand}" ToolTipService.ToolTip="Previous Page"/>
                <Button Content="&gt;" Command="{Binding NextPageCommand}" ToolTipService.ToolTip="Next Page"/>
            </StackPanel>
        </StackPanel>

        <!-- Product List -->
        <ListView Grid.Row="3" ItemsSource="{Binding Products}" SelectionMode="None">
            <ListView.Header>
                <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Image" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Product Name" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Category" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Price" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Stock" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="5" Text="Actions" FontWeight="SemiBold" Width="80" TextAlignment="Right"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Image (Placeholder) -->
                        <Image Source="{Binding Image}" Width="48" Height="48" HorizontalAlignment="Left" Stretch="UniformToFill"/>

                        <!-- Product Name -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ProductId}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>

                        <!-- Category -->
                        <Border Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left" 
                                BorderBrush="Gray" BorderThickness="1" CornerRadius="12" Padding="8,2">
                            <TextBlock Text="{Binding CategoryName}" FontSize="12"/>
                        </Border>

                        <!-- Price -->
                        <TextBlock Grid.Column="3" Text="{Binding Price, Converter={StaticResource PriceConverter}}" VerticalAlignment="Center" FontWeight="SemiBold"/>

                        <!-- Stock -->
                         <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                            <TextBlock Text="{Binding Stock}" FontWeight="SemiBold"/>
                            <Border Background="#D1FAE5"  CornerRadius="4" Padding="6,2">
                                <TextBlock Text="In Stock" Foreground="#065F46" FontSize="11" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>

                        <!-- Actions -->
                        <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                             <Button Background="Transparent" Padding="4">
                                <FontIcon Glyph="&#xE7B3;" FontSize="14"/>
                            </Button>
                            <Button Background="Transparent" Padding="4" Command="{Binding ElementName=PageRoot, Path=DataContext.UpdateProductCommand}" CommandParameter="{Binding}">
                                <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                            </Button>
                             <Button Background="Transparent" Padding="4" Command="{Binding ElementName=PageRoot, Path=DataContext.DeleteProductCommand}" CommandParameter="{Binding}">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="Red"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                 <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
