<Page
    x:Class="MyShop_Frontend.Views.Pages.CustomerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyShop_Frontend.ViewModels.Customers"
    xmlns:converters="using:MyShop_Frontend.Helpers.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">



    <Page.Resources>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ContentDialog x:Name="CustomerDialog"
                       Title="Customer Details"
                       PrimaryButtonText="Save"
                       CloseButtonText="Cancel"
                       DefaultButton="Primary"
                       PrimaryButtonCommand="{Binding SaveCustomerCommand}">
            <StackPanel Spacing="12" Width="350">
                <TextBox Header="Full Name" Text="{Binding FormFullName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Header="Phone" Text="{Binding FormPhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Header="Address" Text="{Binding FormAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </ContentDialog>

        <!-- Header Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="8" Margin="0,0,16,0">
                <TextBox PlaceholderText="Search Phone..." Width="150" 
                         Text="{Binding SearchPhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox PlaceholderText="Search Name..." Width="180" 
                         Text="{Binding SearchName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            
            <Button Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0" Command="{Binding OpenAddDialogCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Add Customer" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2" VerticalAlignment="Center" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8B5;" FontSize="16" />
                    <TextBlock Text="Import" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            
            <Button Grid.Column="3" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                    <TextBlock Text="Export CSV" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Header Row -->
        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Height="40" Margin="0,0,12,0" CornerRadius="4">
            <Grid Padding="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="1" Text="Contact Info" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="2" Text="Points" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="3" Text="Tier" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="4" Text="" VerticalAlignment="Center" FontWeight="SemiBold" Width="40"/>
            </Grid>
        </Border>

        <!-- Data List -->
        <ListView Grid.Row="2" x:Name="CustomerList"
                  ItemsSource="{Binding Customers}" 
                  SelectionMode="None"
                  Padding="0,0,12,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Name -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding CustomerId}" Foreground="Gray" FontSize="10"/>
                        </StackPanel>

                        <!-- Contact Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Address}" Foreground="Gray" FontSize="12"/>
                            <TextBlock Text="{Binding Phone}" FontSize="12"/>
                        </StackPanel>
                        
                         <!-- Points -->
                        <TextBlock Grid.Column="2" Text="{Binding Point}" VerticalAlignment="Center" FontSize="12"/>

                        <!-- Group (Tier) -->
                        <Border Grid.Column="3" Background="LightGray" CornerRadius="12" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding TierName}" Foreground="Black" FontSize="12" FontWeight="Medium"/>
                        </Border>

                        <!-- Actions -->
                        <Button Grid.Column="4" Background="Transparent" BorderThickness="0" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE712;"/>
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Edit" Command="{Binding DataContext.OpenEditDialogCommand, ElementName=CustomerList}" CommandParameter="{Binding}"/>
                                    <MenuFlyoutItem Text="Delete" Foreground="Red"/>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Pagination -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding ShowingStatus}" VerticalAlignment="Center" Foreground="Gray"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button Command="{Binding PreviousPageCommand}" Content="Previous"/>
                <Button Command="{Binding NextPageCommand}" Content="Next"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
