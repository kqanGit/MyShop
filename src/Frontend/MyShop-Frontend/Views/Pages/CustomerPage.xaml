<Page
    x:Class="MyShop_Frontend.Views.Pages.CustomerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyShop_Frontend.ViewModels.Customers"
    xmlns:converters="using:MyShop_Frontend.Helpers.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:CustomerViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <converters:DateToFormattedStringConverter x:Key="DateConverter"/>
        <converters:TierToColorConverter x:Key="TierColorConverter"/>
        <converters:TierToNameConverter x:Key="TierNameConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox PlaceholderText="Search customers..." Width="300" VerticalAlignment="Center" Margin="0,0,16,0"/>

            <Button Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE716;"/>
                    <TextBlock Text="All Groups"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="3" VerticalAlignment="Center" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;"/>
                    <TextBlock Text="Import/Add"/>
                </StackPanel>
            </Button>
            
            <Button Grid.Column="4" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE896;"/>
                    <TextBlock Text="Export CSV"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Header Row -->
        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Height="40" Margin="0,0,12,0" CornerRadius="4">
            <Grid Padding="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2.5*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="1" Text="Contact Info" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="2" Text="Date Joined" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="3" Text="Group" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="4" Text="" VerticalAlignment="Center" FontWeight="SemiBold" Width="40"/>
            </Grid>
        </Border>

        <!-- Data List -->
        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Customers}" 
                  SelectionMode="None"
                  Padding="0,0,12,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Name -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding CustomerId}" Foreground="Gray" FontSize="10"/>
                        </StackPanel>

                        <!-- Contact Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Address}" Foreground="Gray" FontSize="12"/>
                            <TextBlock Text="{Binding Phone}" FontSize="12"/>
                        </StackPanel>

                        <!-- Date Joined -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE787;" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="{Binding CreateDate, Converter={StaticResource DateConverter}}" FontSize="12"/>
                        </StackPanel>

                        <!-- Group -->
                        <Border Grid.Column="3" Background="{Binding TierId, Converter={StaticResource TierColorConverter}}" CornerRadius="12" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding TierId, Converter={StaticResource TierNameConverter}}" Foreground="Black" FontSize="12" FontWeight="Medium"/>
                        </Border>

                        <!-- Actions -->
                        <Button Grid.Column="4" Background="Transparent" BorderThickness="0" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE712;"/>
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Edit"/>
                                    <MenuFlyoutItem Text="Delete" Foreground="Red"/>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
